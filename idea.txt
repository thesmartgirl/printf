if flag_minus
  left allign (ALWAYS APPLIED)

if num_digits > precision
  precision = 0

if precision_set
  if precision > field_width
    precision applies --> str_len = precision, pad = 0
  if precision < field_width
    if flag_zero
    {
      flag_zero ignored
      field_width applies (padding with spaces)
    } --> str_len = field_width, pad = ' '

if !precision_set
  if num_digits > field_width
    ft_putnbr_fd()
  else
    if flag_minus
      print to left and pad with ' ' until field_width
    else if flag_zero
      pad with 0 until field_width
    else
      print to right and pad with ' ' until field_width
